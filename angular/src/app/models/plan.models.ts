export interface Plan {
    ffm_id: number;
    hios_id: string;
    name: string;
    effective: string;
    termination: string;
    premium: string;
    deductible: string;
    max_payout: string;
    dependents: string;
}